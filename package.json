{
  "name": "shotopt",
  "private": true,
  "description": "Dev tooling only — the app itself is a single dependency-free index.html.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8787",
    "test": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
